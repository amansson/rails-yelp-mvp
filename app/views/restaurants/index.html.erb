<% @restaurants.each do |rest| %>
  <h2><%= link_to rest.name, restaurant_path(rest) %></a></h2>
  <h4><%= rest.category %></h4>
  <p><%= rest.address %></p>
  <p><%= rest.phone_number %></p>
<% end %>